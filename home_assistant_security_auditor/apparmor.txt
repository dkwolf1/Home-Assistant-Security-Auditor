#include <tunables/global>

profile home_assistant_security_auditor flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>
  #include <abstractions/nameservice>
  #include <abstractions/ssl_certs>

  capability net_raw,
  capability net_bind_service,

  network inet stream,
  network inet6 stream,

  /run.sh rix,
  /app/** r,
  /usr/bin/python3 rix,
  /usr/lib/python*/** r,
  /usr/lib/libpython*.so* mr,
  /etc/ssl/** r,
  /data/options.json r,
  /homeassistant/** r,
}

