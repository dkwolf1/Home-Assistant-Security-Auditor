configuration:
  public_url:
    name: Public URL
    description: Public Home Assistant URL or hostname to audit.
  scan_common_ports:
    name: Scan common ports
    description: Check if common ports appear reachable from the add-on container.
  request_timeout_seconds:
    name: Request timeout
    description: Timeout in seconds for network checks.

